# Defaults for all vectors under test-vectors/.
# Individual vectors can override via their own [config] or [expected].
# See test-vectors/README.md and docs/adr/snapshot_testing_for_test_vectors.md.

[config]
# Uncomment to set default pipeline (else binoc-stdlib default is used):
# comparators = ["binoc.zip", "binoc.directory", "binoc.csv", "binoc.text", "binoc.binary"]
# transformers = ["binoc.move_detector", "binoc.copy_detector", "binoc.column_reorder_detector"]

[expected]
# Optional default assertions (vectors can override)
